% ********** MAIN PACKAGES
% Load core style
\usepackage{./styles/stylecore}

% Include babel (Standard)
% The primary language is the one on the right
\usepackage[english,portuguese]{babel}

% Manipulation of titles (chapters, sections, ...)
\usepackage[explicit]{titlesec}

% Include images in project
\usepackage{graphicx}

% Include links to pages from Table of Contents
\usepackage[linktocpage=true]{hyperref}

% Include captions (Standard)
\usepackage[centerlast]{caption}

% Include underline utility \uline
\usepackage[normalem]{ulem}

% Include inconsolata monospace font
\usepackage{inconsolata}

% Manipulate headers and footnotes
\usepackage{fancyhdr}





% ********** MATH PACKAGES
% Include AMS's math utils (Standard)
\usepackage[centertags,sumlimits,intlimits,namelimits]{amsmath}

% Enhance amsmath (Standard)
\usepackage{mathtools}

% Include extra math symbols (Standard)
\usepackage{amssymb}

% Include extra math fonts (such as BlackBoard) (Standard)
\usepackage{amsfonts}

% Include AMS's theorem utility (includes proof environment)
\usepackage{amsthm}





% ********** SETUP
\graphicspath{{./images/}{./}{./contents/}}

\geometry{
    ignoreheadfoot,
    %hdivide = {,,},
    %vdivide = {,,},
    heightrounded
}

%\addto\captionsportuguese{\renewcommand{\contentsname}{Conteúdos}}
%\addto\captionsenglish{\renewcommand{\contentsname}{Contents}}





% ********** DOCUMENT'S PENALTIES
\allowdisplaybreaks
\hyphenpenalty=100
\binoppenalty=3000
\relpenalty=500
\clubpenalty=3000
\widowpenalty=7000
\displaywidowpenalty=10000
\brokenpenalty=2000
\predisplaypenalty=8000





% ********** DOCUMENT'S ENVIRONMENTS





% ********** DOCUMENT'S TITLES AND HEADERS
\pagestyle{fancy}

% Left Header
\lhead{}

% Center Header
\chead{Faculdade de Engenharia da Universidade do Porto}

% Right Header
\rhead{}

% Left Footnote
\lfoot{Tema 3 -- Sistema de Evacuação}

% Center Footnote
\cfoot{}

% Right Footnote
\rfoot{\thepage}

\renewcommand*{\headrulewidth}{0.4pt}
\renewcommand*{\footrulewidth}{0.4pt}





% ********** DOCUMENT'S TEXT STYLE COMMANDS
\newcommand*{\bslash}{\textbackslash}

\newcommand*{\file}[1]{\textit{\detokenize{#1}}}

\newcommand*{\folder}[1]{\textsl{\detokenize{#1}}}

\newcommand*{\external}[1]{\textsl{\detokenize{#1}}}

\newcommand*{\mono}[1]{\texttt{\detokenize{#1}}}

\renewcommand*{\sf}[1]{\textsf{\detokenize{#1}}}

\newcommand*{\exe}[1]{\textsf{\detokenize{#1}}}

\newcommand*{\class}[1]{\textsf{\detokenize{#1}}}

\newcommand*{\method}[1]{\textsf{\detokenize{#1}}}

\newcommand*{\function}[1]{\textsf{\detokenize{#1}}}

\newcommand*{\regex}[1]{\texttt{#1}}





% ********** DOCUMENT'S MATH COMMANDS
\newcommand*{\sfmath}[1]{\ensuremath{\textup{\textsf{\detokenize{#1}}}}}

\newcommand*{\monomath}[1]{\ensuremath{\textup{\texttt{\detokenize{#1}}}}}

%\newcommand*{\suchthat}{\ensuremath{\,:\,}}

%\newcommand*{\coprime}{\ensuremath{\perp}}

%\newcommand*{\mdc}{\ensuremath{\text{mdc}}}

%\newcommand*{\mmc}{\ensuremath{\text{mmc}}}

